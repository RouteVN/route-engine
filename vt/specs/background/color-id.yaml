---
title: Background Color ID
description: |
  Background backing color should use screen.backgroundColor by default, then
  resolve background.colorId through resources.colors and persist that color
  behind later background resource updates that omit colorId.

  - the first screenshot should show screen.backgroundColor around the transformed background image
  - the second screenshot should show the authored background.colorId backing color
  - the third screenshot should keep background.colorId after changing only the background image
skipInitialScreenshot: true
steps:
  - action: screenshot
  - action: customEvent
    name: vt:nextLine
  - action: screenshot
  - action: customEvent
    name: vt:nextLine
  - action: screenshot
---
screen:
  width: 1920
  height: 1080
  backgroundColor: "#3A123A"
resources:
  images:
    bg-forest-card:
      fileId: dmni32
      width: 640
      height: 360
    bg-door-card:
      fileId: lakjf3lka
      width: 640
      height: 360
  transforms:
    upper-left-card:
      x: 160
      y: 120
      anchorX: 0
      anchorY: 0
      rotation: 0
      scaleX: 1
      scaleY: 1
    lower-right-card:
      x: 1760
      y: 960
      anchorX: 1
      anchorY: 1
      rotation: 0
      scaleX: 1
      scaleY: 1
  colors:
    backingBlue:
      hex: "#123A5A"
story:
  initialSceneId: backgroundScene
  scenes:
    backgroundScene:
      name: Background Scene
      initialSectionId: main
      sections:
        main:
          lines:
            - id: line1
              actions:
                background:
                  resourceId: bg-forest-card
                  transformId: upper-left-card
            - id: line2
              actions:
                background:
                  colorId: backingBlue
                  resourceId: bg-door-card
                  transformId: lower-right-card
            - id: line3
              actions:
                background:
                  resourceId: bg-forest-card
                  transformId: upper-left-card
